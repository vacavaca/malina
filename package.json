{
  "name": "malina-parent",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "commit": "git-cz",
    "clean": "lerna clean -y && lerna exec 'rm -rf dist'",
    "release": "lerna clean -y && lerna bootstrap --hoist && lerna run --bail --stream release && lerna publish"
  },
  "author": "vacavaca <vacavaca@fastmail.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/vacavaca/malina.git"
  },
  "bugs": {
    "url": "https://github.com/vacavaca/malina/issues"
  },
  "license": "MIT",
  "devDependencies": {
    "cz-conventional-changelog": "^2.1.0",
    "lerna": "^3.5.1"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
