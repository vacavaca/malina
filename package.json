{
  "name": "malina-parent",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "lerna clean -y && lerna exec 'rm -rf dist'",
    "release": "lerna bootstrap --hoist && lerna run --bail release && lerna publish"
  },
  "author": "vacavaca <vacavaca@fastmail.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/vacavaca/malina.git"
  },
  "bugs": {
    "url": "https://github.com/vacavaca/malina/issues"
  },
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.5.1"
  }
}
